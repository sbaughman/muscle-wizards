<div class="backgrnd">
  <h4>Weight</h4>
  <% if weights.any? %>
    <ul class="no-bullet">
      <li><h5>Current Weight: <%= weights.last.display_weight %></h5></li>
      <li><h5>Start Weight: <%= weights.first.display_weight %></h5></li>
      <li><h5><%= time_ago_in_words(weights.first.created_at) %> ago</h5></li>
      <li><%= link_to "Log new weight", new_prep_bodyweight_path(@prep) if @prep.athlete == current_user %></li>
      <li><%= link_to "Weight history", prep_bodyweights_path(@prep) %></li>
    </ul>
  <% else %>
    <h5 class="no-data">No weigh-ins recorded yet</h5>
    <ul class="no-bullet">
      <li><%= link_to "Log new weight", new_prep_bodyweight_path(@prep) if @prep.athlete == current_user %></li>
    </ul>
  <% end %>

</div>
