<div class="backgrnd">
  <h4>Cardio</h4>
  <% if cardio.any? %>
    <ul class="no-bullet">
      <li><h5>Duration: <%= cardio.last.duration %> minutes</h5></li>
      <li><h5>Activity: <%= cardio.last.activity %></h5></li>
      <li><h5>Type: <%= cardio.last.style %></h5></li>
      <li><%= link_to "Log today's cardio", new_prep_cardio_path(@prep) if @prep.athlete == current_user %></li>
      <li><%= link_to "Set new cardio goal", new_prep_target_cardio_path(@prep) if @prep.coach_id == current_user.id %></li>
      <li><%= link_to "Cardio History", prep_cardios_path(@prep) %></li>
    </ul>
  <% else %>
    <h5 class="no-data">No cardio set yet</h5>
    <ul class="no-bullet">
      <li><%= link_to "Log today's cardio", new_prep_cardio_path(@prep) if @prep.athlete == current_user %></li>
      <li><%= link_to "Set new cardio goal", new_prep_target_cardio_path(@prep) if @prep.coach_id == current_user.id %></li>
      <li><%= link_to "Cardio History", prep_cardios_path(@prep) if @prep.cardios.any? %></li>
    </ul>
  <% end %>
</div>
