<%= link_to(image_tag(photo.image(:thumb)), photo.image(:original), title: "#{photo.created_at.strftime('%D')} - #{photo.tag.name if photo.tag} - #{time_til_contest(photo)} out from #{nearest_contest.title} #{link_to "Edit", edit_prep_photo_path(@prep, photo)}") %>
