<%= link_to(image_tag(photo.image(:thumb)), photo.image(:original), title: "#{photo.created_at.strftime('%D')} - #{photo.tag.name if photo.tag} - #{time_til_contest(photo)} out from #{nearest_contest.title}") %>
