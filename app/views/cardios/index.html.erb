<div class="row">
  <%= link_to "Add new cardio", new_prep_cardio_path(@prep), class: "button" %>

  <%= link_to "Back to Prep Home", prep_path(@prep), class: "button" %>
  <div class="small-12 medium-8 medium-offset-2 columns">
    <%= render partial: 'cardio_chart' %>
  </div>


  <ul>
    <%= render @cardios %>
  </ul>
</div>
